# ===========================================================================
# Wicara — dependensi
#
# WAJIB Python 3.11 resmi dari python.org. Jangan asal panggil `python` dari
# PATH: MSYS2 Python atau 3.14 akan gagal karena wheel PyTorch CUDA Windows
# tidak kompatibel.
#
#   py -3.11 -m venv .venv
#
# PyTorch di-install TERPISAH karena butuh index khusus CUDA. Sesuaikan
# cu130 dengan driver GPU Anda — cek https://pytorch.org/get-started/locally/
# (cu130 dipakai di sini karena driver 581.86 mendukung CUDA 13.0):
#
#   .venv\Scripts\python.exe -m pip install torch==2.13.0+cu130 \
#       --index-url https://download.pytorch.org/whl/cu130
#
# Versi dipatok persis (==) dan bukan minimum (>=). Repo ini adalah research
# artifact: angka yang dilaporkan di README harus bisa direproduksi, dan
# `>=` berarti enam bulan lagi orang lain menjalankan stack yang berbeda.
# ===========================================================================

# --- Pipeline data ---
numpy==2.4.6             # memmap uint16 untuk train.bin/val.bin
pyarrow==25.0.1          # baca parquet per batch tanpa memuat penuh ke RAM
datasets==5.0.1          # streaming korpus besar dari HuggingFace

# --- Pengujian ---
pytest==9.1.1            # 105 uji di tests/

# ===========================================================================
# Sengaja TIDAK dipakai, supaya tidak ada dependensi yang menumpang:
#
#   datasketch   -> deduplikasi memakai Bloom filter numpy buatan sendiri
#                   (src/data/dedup.py), RAM tetap untuk ratusan juta item
#   fasttext     -> deteksi bahasa memakai heuristik kata fungsi
#                   (src/data/clean.py), jauh lebih cepat dan tanpa model
#   tensorboard  -> logging memakai JSONL biasa (src/train/checkpoint.py),
#                   satu baris per event dan aman kalau training mati mendadak
#   regex        -> pola yang dipakai cukup dengan `re` bawaan Python
#   tqdm         -> progress dicetak manual dengan throughput dan ETA
#
# Akan ditambahkan di Fase 2 (tokenizer):
#   tokenizers   -> training BPE 16k (backend Rust)
# ===========================================================================
